{
    "builds": [{
        "src": "config/asgi.py",
        "use": "@vercel/python",
        "config": { "maxLambdaSize": "60mb", "runtime": "python3.9" }
    },
    {
        "src": "build.sh",
        "use": "@vercel/static-build"
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "config/asgi.py"
        }
    ]
}